# Generated QA artifacts: keep the directory + the committed sample report & spot-check, ignore the
# rest (raw TSVs and per-repo reports are reproducible via qa/scripts/qa-deps-accuracy.sh).
*
!.gitignore
!tiko-trust-rule-engine-report.md
!tiko-trust-rule-engine-spotcheck.md
