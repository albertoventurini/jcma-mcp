# Generated QA artifacts are reproducible via qa/scripts/qa-deps-accuracy.sh and depend on the host
# (paths, timings), so they are not committed — keep only the directory and the hand-written
# spot-check (authored analysis, not regenerated by the harness). The auto report, raw TSVs, and
# per-type diffs are all ignored.
*
!.gitignore
!tiko-trust-rule-engine-spotcheck.md
