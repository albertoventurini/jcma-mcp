package com.example;

/** Test class discovered by the src/test/java convention with no pom present (Gradle/plain). */
public class WidgetTest {
    public void hasSize() {
        new Widget().size();
    }
}
