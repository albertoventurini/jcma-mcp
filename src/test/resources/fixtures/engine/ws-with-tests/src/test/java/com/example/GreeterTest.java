package com.example;

/** A test class (under src/test/java) — must be indexed and tagged TEST. */
public class GreeterTest {
    public void greetsByName() {
        new Greeter().greet("world");
    }
}
