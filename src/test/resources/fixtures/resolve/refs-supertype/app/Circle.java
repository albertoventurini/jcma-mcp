package app;

/** Implements {@code Shape} — contributes an incoming IMPLEMENTS hierarchy edge to {@code Shape}. */
public class Circle implements Shape {
    public void draw() {
        // body irrelevant
    }
}
