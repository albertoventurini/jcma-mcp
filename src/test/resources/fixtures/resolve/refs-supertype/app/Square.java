package app;

/** A second implementor — a second incoming IMPLEMENTS hierarchy edge to {@code Shape}. */
public class Square implements Shape {
    public void draw() {
        // body irrelevant
    }
}
