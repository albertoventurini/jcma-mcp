package app;

/** A method ({@code solo()}) used from exactly one file ({@code Target.bootstrap}) — the rare-name case. */
public class Lonely {

    public void solo() {
        // body irrelevant
    }
}
