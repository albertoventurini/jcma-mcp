// jcma — agent-native Java code-intelligence engine (M1 core engine + index).
// Gradle (Kotlin DSL); chosen for the mature GraalVM native-image plugin (PRD §11, M1 decision).
rootProject.name = "jcma"
