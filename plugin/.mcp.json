{
  "mcpServers": {
    "jcma": {
      "type": "stdio",
      "command": "${CLAUDE_PLUGIN_ROOT}/scripts/jcma-launch.sh",
      "args": ["serve"],
      "env": {
        "CLAUDE_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}",
        "CLAUDE_PLUGIN_DATA": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
